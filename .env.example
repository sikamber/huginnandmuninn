ANTHROPIC_API_KEY=your_key_here
DOMAIN=yourdomain.com  # use :80 for local testing
AUTH_USER=your_username
AUTH_HASH=your_bcrypt_hash  # generate with: docker run caddy:2-alpine caddy hash-password --plaintext 'yourpassword'
